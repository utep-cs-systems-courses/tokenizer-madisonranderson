#include <stdlib.h>
#include <stdio.h>
#include "tokenizer.h"
#include "history.h"

int main()
{
  char userIn[100];

  //while(1){
    // putchar('$');
    // fgets(userIn, 100 stdin);
    // printf("%s\n", userIn);
    //}
  return 0;
}
